import { defineStyleConfig } from '@chakra-ui/react';

const Tooltip = defineStyleConfig({
  baseStyle: {
    bg: 'dolbyNeutral.500',
    borderRadius: '6px',
    color: 'white',
    fontSize: '12px',
  },
});

export default Tooltip;
